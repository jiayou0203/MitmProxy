一、签名校验问题
1.1：后端未对签名 signature 做校验，故可直接篡改载荷 payload 内容，并可一并删除 singature 签名字段来尝试绕过签名校验
1.2：将 alg 值置为 none，若服务器支持 none 算法，便可绕过签名校验
1.3:将生成的 jwt 字符串(可删除结尾的.) 替换原有的字符串测试即可
二、弱密钥暴力破解
2:当 alg 值为 HMAC 类对称加密算法时，可以针对密钥进行暴力破解
三、kid指定攻击
3.kid 即为 key ID ，存在于 jwt header 头部中，是一个可选的字段，用来指定加密算法的密钥
{
    "alg": "HS256",
    "typ": "JWT",
    "kid": "xxx"
}
若服务器并未对 header 头部做限制，那么程序将会按照 header 中指定的密钥进行校验，从而通过签名校验
四、非对称加密算法转对称加密算法
4.1:当 header 头部指定签名算法为 RSA 非对称加密算法时，可以替换为 HMAC 对称加密算法，并且通过获取到的公钥重新做签名
4.2:服务器在签名校验时便可能会使用公钥做校验